<?xml version="1.0" encoding="utf-8"?>

<TableLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">
    <TableRow>
        <TextView
            android:text="@string/city_patient_title_text"
            android:layout_span="2"/>
    </TableRow>
    <TableRow>
        <TextView
            android:text="@string/state_patient_text"/>
        <Spinner
            android:layout_width="@dimen/address_item_width"
            android:id="@+id/spinnerStatePatient"
            android:layout_height="wrap_content"
            android:spinnerMode="dropdown">

        </Spinner>
    </TableRow>
    <TableRow>
        <TextView
            android:text="@string/city_patient_text"/>
        <Spinner
            android:layout_width="@dimen/address_item_width"
            android:id="@+id/spinnerCityPatient"
            android:layout_height="wrap_content"
            android:spinnerMode="dropdown" />
    </TableRow>
</TableLayout>